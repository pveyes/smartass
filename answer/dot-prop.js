/**
 * Given an object, and a string 'accessor', get the value inside that object.
 * Return undefined if value not found
 *
 * Sample input object: { a: { b: 2 }}
 * Sample input accessor: 'a.b'
 * Sample output: 2
 *
 * @param Object object
 * @param String accessor
 * @return Any
 */

module.exports = function dotProp(object, accessor) {
  var value = undefined;
  return value;
};
