/**
 * Given a sentences, write a function that will rewrite those sentences backward.
 * The word itself must not be reversed, only reverse the order of the word inside
 * the sentences. A sentence is a list of word separated by space that ends with
 * period (".") character. Every input is guaranteed to be a valid sentence (it will
 * have at least one period character in the end). Every input will only contains
 * a word and a period, no other character exists.
 *
 * Sample input: "Lorem ipsum. Dolor sit amet."
 * Sample output: "amet sit Dolor. ipsum Lorem."
 *
 * @param String sentences
 * @return String output
 */

module.exports = function sayBackward(sentences) {
  return sentences;
};
