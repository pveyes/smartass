/**
 * Given a hex-formatted color, return an array that consist of rgb value
 * of that color. Input can be a 3-chars hex or 6-chars hex and always
 * started with a pound sign (#)
 *
 * Sample input: "#ff0055"
 * Sample output: [255, 0, 85]
 *
 * @param String hex
 * @return Array.<number> rgb
 */

module.exports = function hexToRgb(hex) {
  var r = 0;
  var g = 0;
  var b = 0;
  return [r, g, b];
};
