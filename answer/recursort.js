/**
 *
 * Given multidimensional array of numbers, sort all number inside
 * the array. Array should appear after all numbers. If there is multiple
 * Array, place the longest array last. If the length is array is same, compare
 * the last element, if still same, compare previous element, and so on.
 *
 * Example
 * Input: [[5, 4], 3, 2, 1]
 * Output: [1, 2, 3, [4, 5]]
 *
 * Input: [[5, 4], [3], 2, 1]
 * Output: [1, 2, [3], [4, 5]]
 *
 * Input: [[5, 4], [3, 2], 1]
 * Output: [1, [2, 3], [4, 5]]
 *
 * @param {Array}
 * @return {Array}
 */
module.exports = function recursort(array) {
  return array;
};
