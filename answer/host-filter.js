/**
 * You will be given input array of object, containing host
 * information. Your job is to filter them
 *
 * Sample input: [
 *   { k: 'api01', v: 1 }
 *   { k: 'api02', v: 2 }
 * ]
 *
 * Sample output: [
 *   { k: 'api01', v: 1 }
 * ]
 *
 * Possible host: api, queue
 * Note: if the host is staging, include them all
 * @param Array input
 * @return Array output
 */
module.exports = function hostFilter(input) {
  const output = [];
  return output;
};
